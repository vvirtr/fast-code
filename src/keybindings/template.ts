/**
 * Keybindings template generator stub.
 *
 * User keybinding customization has been removed. The template generator
 * is no longer needed. Returns an empty string for backward compatibility.
 */

/**
 * Generate a template keybindings.json file content.
 * Returns empty string — user customization is removed.
 */
export function generateKeybindingsTemplate(): string {
  return ''
}
